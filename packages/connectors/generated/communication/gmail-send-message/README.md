# Catalyst Gmail - Send Email Connector

Send an email using Gmail

## Installation

### 1. Import n8n Workflow

1. Open your n8n instance
2. Click **Workflows** > **Import from File**
3. Select `gmail-send-message.n8n.json`
4. **Configure credentials** for Gmail in n8n
5. **Activate** the workflow

Webhook URL: `http://catalyst-n8n:5678/webhook/catalyst-gmail-send-message`

### 2. Deploy Element Template

Copy `gmail-send-message.element.json` to your Camunda Modeler templates directory.

## Input Parameters

| Variable | Type | Required | Description |
|----------|------|----------|-------------|
| `to` | string | Yes | Email address of the recipient |
| `subject` | string | Yes | Subject line of the email |
| `message` | string | Yes | The body content of the email (plain text or HTML) |
| `cc` | string | No | Email addresses to CC (comma-separated) |
| `bcc` | string | No | Email addresses to BCC (comma-separated) |

## Output Variables

| Variable | Type | Description |
|----------|------|-------------|
| `success` | Boolean | Whether the request succeeded |
| `statusCode` | Number | HTTP status code |
| `responseBody` | Object | Response data from Gmail API |
| `error` | String | Error message if failed |

## Usage Example

1. Deploy the example BPMN process (`gmail-send-message.bpmn`)
2. Start a new process instance
3. Fill in the required input parameters in the user task
4. The service task will call Gmail via n8n

## Files

- `gmail-send-message.n8n.json` - n8n webhook workflow
- `gmail-send-message.element.json` - Camunda Modeler template
- `gmail-send-message.bpmn` - Example BPMN process
- `README.md` - This documentation
- `connector.json` - Connector metadata

## Configuration

### n8n Credentials

This connector requires Gmail credentials to be configured in n8n:

- `gmailOAuth2`

Configure credentials in n8n under **Settings** > **Credentials** > **Add Credential**.

### Webhook URL Allowlist

Ensure your Catalyst Bridge allows the webhook URL prefix:
```
http://catalyst-n8n:5678/webhook/
```

---

*Generated by Catalyst Connector Generator*
